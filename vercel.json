{
  "buildCommand": "npx prisma migrate resolve --rolled-back 20260625160000_phase_9_multitenant || true ; npx prisma generate && npx prisma migrate deploy && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"]
}
