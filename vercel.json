{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx tsx scripts/seed_checklist.ts && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"]
}
