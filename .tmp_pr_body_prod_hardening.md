## Summary
- remove tracked preview env artifact and replace it with a safe committed template
- replace the risky Vercel `db push --accept-data-loss` build path with `prisma migrate deploy`
- lock down nested `/api/health/*` diagnostics/migration routes and stop exposing them as public middleware exceptions
- remove hardcoded company fallback IDs from approvals flows
- add centralized API permission/company-context helpers and enforce them on key operational APIs
- make module gating work with both array-based and object-based company module control storage

## Verification
- `git diff --check`
- `npx eslint src/lib/auth/require-module.ts src/lib/auth/require-super-admin.ts src/lib/auth/require-api-permission.ts src/actions/approvals.ts src/proxy.ts src/app/api/sites/route.ts src/app/api/expenses/route.ts src/app/api/attendance/route.ts src/app/api/approvals/route.ts src/app/api/approvals/[id]/route.ts src/app/api/health/db-force-migrate/route.ts src/app/api/health/db-check/route.ts src/app/api/health/cleanup-activity/route.ts src/app/api/health/fix-templates/route.ts`

## Production risk reduced
- no committed preview env export/token artifact in repo
- no public nested health/migration/cleanup endpoints
- no hardcoded approval company fallback
- no deploy-time `db push --accept-data-loss`
- stronger permission/module enforcement on high-risk APIs
